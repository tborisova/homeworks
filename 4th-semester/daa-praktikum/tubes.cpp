#include <iostream>
#include <algorithm>
#include <cstdio>
using namespace std;

int main(){
  int tests, k, n;
  long tubes[10000];
  scanf("%d", &tests);

  while(tests){

    scanf("%d %d", &n, &k);

    for(int i = 0; i < n; i++){
      scanf("%ld", &tubes[i]);
      
    }

    sort(tubes, tubes + n);

    rigth = 0;
    left = 

    tests--;
  }
}