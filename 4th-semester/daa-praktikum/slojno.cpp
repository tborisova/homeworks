#include <iostream>
#include <cstdio>
#include <vector>
#include <algorithm>

using namespace std;

int main(){
  int n, a, counter = 0, temp, el1_i, el2_i;

  scanf("%d", &n);

  int numbers[n], position_to_be_changed;

  for(int i=0; i < n; i++){
    scanf("%d", &numbers[i]);
  }


}